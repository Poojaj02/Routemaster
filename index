<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>RouteMaster – Tours & Cab Services</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700;900&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --cream: #FAF7F2;
    --dark: #1A1209;
    --gold: #C8973A;
    --gold-light: #E8B55A;
    --rust: #B84A1E;
    --green: #0F2044;
    --green-light: #1E3A6E;
    --text: #2C1F0E;
    --text-muted: #6B5840;
    --border: rgba(200, 151, 58, 0.2);
    --shadow: 0 8px 40px rgba(26,18,9,0.12);
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--cream);
    color: var(--text);
    overflow-x: hidden;
    cursor: none;
  }

  /* CUSTOM CURSOR */
  .cursor {
    position: fixed;
    width: 14px; height: 14px;
    background: var(--gold);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    transition: transform 0.08s ease, width 0.2s, height 0.2s, background 0.2s;
    mix-blend-mode: multiply;
  }
  .cursor-ring {
    position: fixed;
    width: 40px; height: 40px;
    border: 1.5px solid var(--gold);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9998;
    transform: translate(-50%, -50%);
    transition: transform 0.18s ease;
    opacity: 0.6;
  }
  a:hover ~ .cursor, button:hover ~ .cursor { transform: translate(-50%,-50%) scale(2); }

  /* NAV */
  nav {
    position: fixed; top: 0; left: 0; right: 0;
    z-index: 1000;
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 5vw;
    height: 72px;
    background: rgba(250,247,242,0.92);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
    transition: box-shadow 0.3s;
  }
  nav.scrolled { box-shadow: 0 4px 30px rgba(26,18,9,0.08); }

  .logo {
    font-family: 'Playfair Display', serif;
    font-size: 1.65rem;
    font-weight: 900;
    letter-spacing: -0.5px;
    text-decoration: none;
  }
  .logo span:first-child { color: var(--dark); }
  .logo span:last-child { color: var(--gold); }

  .nav-links { display: flex; gap: 2.2rem; align-items: center; }
  .nav-links a {
    font-size: 0.88rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text-muted);
    text-decoration: none;
    position: relative;
    transition: color 0.2s;
  }
  .nav-links a::after {
    content: '';
    position: absolute; bottom: -3px; left: 0; right: 0;
    height: 1.5px; background: var(--gold);
    transform: scaleX(0); transform-origin: right;
    transition: transform 0.3s ease;
  }
  .nav-links a:hover { color: var(--dark); }
  .nav-links a:hover::after { transform: scaleX(1); transform-origin: left; }

  .nav-cta {
    background: var(--gold);
    color: #fff !important;
    padding: 0.5rem 1.3rem;
    border-radius: 4px;
    font-weight: 600 !important;
    transition: background 0.2s, transform 0.15s !important;
  }
  .nav-cta::after { display: none !important; }
  .nav-cta:hover { background: var(--rust) !important; transform: translateY(-1px); }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    padding: 72px 5vw 0;
    position: relative;
    overflow: hidden;
    gap: 4rem;
  }

  .hero-bg {
    position: absolute; inset: 0;
    background: linear-gradient(135deg, #FAF7F2 55%, #F0E8D8 100%);
    z-index: 0;
  }
  .hero-bg::before {
    content: '';
    position: absolute;
    width: 600px; height: 600px;
    top: -100px; right: -100px;
    background: radial-gradient(circle, rgba(200,151,58,0.12) 0%, transparent 70%);
    border-radius: 50%;
  }
  .hero-bg::after {
    content: '';
    position: absolute;
    width: 400px; height: 400px;
    bottom: 5%; left: -80px;
    background: radial-gradient(circle, rgba(45,80,22,0.08) 0%, transparent 70%);
    border-radius: 50%;
  }

  .hero-content { position: relative; z-index: 1; animation: fadeUp 0.9s ease forwards; }

  .hero-badge {
    display: inline-flex; align-items: center; gap: 0.5rem;
    background: rgba(200,151,58,0.12);
    border: 1px solid var(--border);
    border-radius: 100px;
    padding: 0.35rem 1rem;
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 1.5rem;
  }
  .hero-badge::before { content: '✦'; font-size: 0.6rem; }

  .hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.8rem, 5vw, 4.2rem);
    font-weight: 900;
    line-height: 1.1;
    color: var(--dark);
    margin-bottom: 1.2rem;
  }
  .hero h1 em {
    font-style: normal;
    color: var(--gold);
    position: relative;
    display: inline-block;
  }
  .hero h1 em::after {
    content: '';
    position: absolute; bottom: 2px; left: 0; right: 0;
    height: 3px;
    background: var(--gold);
    border-radius: 2px;
    opacity: 0.4;
  }

  .hero p {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-muted);
    max-width: 440px;
    margin-bottom: 2rem;
  }

  .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; }

  .btn-primary {
    display: inline-flex; align-items: center; gap: 0.5rem;
    background: var(--dark);
    color: #fff;
    padding: 0.85rem 2rem;
    border-radius: 6px;
    font-weight: 600;
    font-size: 0.92rem;
    text-decoration: none;
    transition: background 0.2s, transform 0.15s;
    letter-spacing: 0.02em;
    cursor: none;
  }
  .btn-primary:hover { background: var(--rust); transform: translateY(-2px); }

  .btn-outline {
    display: inline-flex; align-items: center; gap: 0.5rem;
    border: 1.5px solid var(--gold);
    color: var(--gold);
    padding: 0.85rem 2rem;
    border-radius: 6px;
    font-weight: 600;
    font-size: 0.92rem;
    text-decoration: none;
    transition: all 0.2s;
    cursor: none;
  }
  .btn-outline:hover { background: var(--gold); color: #fff; transform: translateY(-2px); }

  .hero-visual {
    position: relative; z-index: 1;
    animation: fadeUp 0.9s 0.2s ease both;
  }

  .search-card {
    background: #fff;
    border-radius: 16px;
    padding: 2rem;
    box-shadow: var(--shadow), 0 0 0 1px var(--border);
    position: relative;
  }
  .search-card h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.25rem;
    margin-bottom: 1.4rem;
    color: var(--dark);
  }

  .search-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0.8rem; }
  .search-field { display: flex; flex-direction: column; gap: 0.3rem; }
  .search-field.full { grid-column: 1 / -1; }
  .search-field label {
    font-size: 0.73rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--text-muted);
  }
  .search-field input, .search-field select {
    border: 1.5px solid #e5ddd0;
    border-radius: 8px;
    padding: 0.7rem 1rem;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.92rem;
    color: var(--text);
    background: var(--cream);
    outline: none;
    transition: border-color 0.2s;
    cursor: none;
  }
  .search-field input:focus, .search-field select:focus { border-color: var(--gold); }

  .search-btn {
    grid-column: 1 / -1;
    background: var(--gold);
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 0.9rem;
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    cursor: none;
    transition: background 0.2s, transform 0.15s;
    letter-spacing: 0.02em;
  }
  .search-btn:hover { background: var(--rust); transform: translateY(-1px); }

  .trust-badges {
    display: flex; gap: 1.5rem;
    margin-top: 1.4rem;
    padding-top: 1.2rem;
    border-top: 1px solid #ede5d8;
  }
  .trust-badge {
    display: flex; align-items: center; gap: 0.4rem;
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--text-muted);
  }
  .trust-badge span { color: var(--green-light); font-size: 1rem; }

  /* SECTION */
  section, .section-wrap {
    padding: 6rem 5vw;
  }

  .section-label {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 0.6rem;
  }

  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(1.8rem, 3vw, 2.6rem);
    font-weight: 800;
    color: var(--dark);
    line-height: 1.2;
    margin-bottom: 0.6rem;
  }

  .section-sub {
    font-size: 1rem;
    color: var(--text-muted);
    max-width: 480px;
    line-height: 1.6;
    margin-bottom: 2.8rem;
  }

  /* TOUR CARDS */
  .tours-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .tour-card {
    border-radius: 14px;
    overflow: hidden;
    position: relative;
    cursor: none;
    group: true;
    aspect-ratio: 4/5;
    box-shadow: 0 4px 20px rgba(26,18,9,0.1);
    transition: transform 0.3s, box-shadow 0.3s;
  }
  .tour-card:hover { transform: translateY(-6px); box-shadow: 0 20px 50px rgba(26,18,9,0.18); }

  .tour-card img {
    width: 100%; height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  .tour-card:hover img { transform: scale(1.05); }

  .tour-overlay {
    position: absolute; inset: 0;
    background: linear-gradient(to top, rgba(26,18,9,0.85) 0%, transparent 50%);
  }

  .tour-info {
    position: absolute;
    bottom: 0; left: 0; right: 0;
    padding: 1.5rem;
    color: #fff;
  }
  .tour-tag {
    display: inline-block;
    background: var(--gold);
    color: #fff;
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.25rem 0.7rem;
    border-radius: 100px;
    margin-bottom: 0.6rem;
  }
  .tour-info h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.35rem;
    font-weight: 700;
    margin-bottom: 0.3rem;
  }
  .tour-info p { font-size: 0.85rem; opacity: 0.8; }

  /* CAB ROUTES */
  .routes-section { background: var(--dark); }
  .routes-section .section-label { color: var(--gold-light); }
  .routes-section .section-title { color: #fff; }
  .routes-section .section-sub { color: rgba(255,255,255,0.55); }

  .routes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
  }

  .route-card {
    border-radius: 14px;
    overflow: hidden;
    position: relative;
    cursor: none;
    transition: transform 0.3s;
    border: 1px solid rgba(255,255,255,0.08);
  }
  .route-card:hover { transform: translateY(-4px); }

  .route-card img {
    width: 100%; height: 200px;
    object-fit: cover;
    display: block;
    filter: brightness(0.85);
    transition: filter 0.3s;
  }
  .route-card:hover img { filter: brightness(1); }

  .route-info {
    background: rgba(255,255,255,0.05);
    backdrop-filter: blur(10px);
    padding: 1.3rem 1.4rem;
  }
  .route-from-to {
    display: flex; align-items: center; gap: 0.8rem;
    margin-bottom: 0.5rem;
  }
  .city { font-size: 1.05rem; font-weight: 600; color: #fff; }
  .route-arrow { color: var(--gold); font-size: 1.1rem; }
  .route-detail { font-size: 0.82rem; color: rgba(255,255,255,0.5); }
  .route-badge {
    display: inline-block;
    background: rgba(200,151,58,0.2);
    color: var(--gold-light);
    font-size: 0.72rem;
    font-weight: 600;
    padding: 0.2rem 0.7rem;
    border-radius: 100px;
    border: 1px solid rgba(200,151,58,0.3);
    margin-top: 0.6rem;
  }

  /* DESTINATIONS MOSAIC */
  .mosaic {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(2, 240px);
    gap: 0.8rem;
  }
  .mosaic-item {
    border-radius: 10px;
    overflow: hidden;
    cursor: none;
    position: relative;
  }
  .mosaic-item:first-child { grid-column: 1; grid-row: 1 / 3; }
  .mosaic-item img {
    width: 100%; height: 100%;
    object-fit: cover;
    transition: transform 0.4s, filter 0.3s;
    filter: brightness(0.9);
  }
  .mosaic-item:hover img { transform: scale(1.06); filter: brightness(1); }

  /* GALLERY */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-auto-rows: 180px;
    gap: 0.8rem;
  }
  .gallery-item {
    border-radius: 10px;
    overflow: hidden;
    cursor: none;
  }
  .gallery-item:nth-child(3) { grid-column: span 2; }
  .gallery-item img {
    width: 100%; height: 100%;
    object-fit: cover;
    transition: transform 0.4s;
  }
  .gallery-item:hover img { transform: scale(1.06); }

  /* GUIDES */
  .guides-section { background: #F5EEE3; }
  .guides-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1.5rem;
  }
  .guide-card {
    background: #fff;
    border-radius: 14px;
    padding: 1.8rem 1.4rem;
    text-align: center;
    box-shadow: 0 2px 16px rgba(26,18,9,0.07);
    transition: transform 0.25s, box-shadow 0.25s;
    cursor: none;
  }
  .guide-card:hover { transform: translateY(-4px); box-shadow: 0 10px 30px rgba(26,18,9,0.12); }
  .guide-card img {
    width: 80px; height: 80px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid var(--gold);
    margin-bottom: 1rem;
  }
  .guide-card h4 {
    font-family: 'Playfair Display', serif;
    font-size: 1.1rem;
    color: var(--dark);
    margin-bottom: 0.25rem;
  }
  .guide-card p { font-size: 0.82rem; color: var(--text-muted); }
  .guide-stars { color: var(--gold); font-size: 0.8rem; margin-top: 0.5rem; }

  /* ABOUT */
  .about-section {
    background: var(--green);
    color: #fff;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
  }
  .about-content .section-label { color: rgba(255,255,255,0.5); }
  .about-content .section-title { color: #fff; }
  .about-content p {
    color: rgba(255,255,255,0.72);
    line-height: 1.75;
    margin-bottom: 1rem;
    font-size: 0.97rem;
  }
  .about-content p strong { color: var(--gold-light); font-weight: 600; }

  .about-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
  .stat-box {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 12px;
    padding: 1.4rem;
  }
  .stat-box .number {
    font-family: 'Playfair Display', serif;
    font-size: 2.2rem;
    font-weight: 900;
    color: var(--gold-light);
    line-height: 1;
    margin-bottom: 0.3rem;
  }
  .stat-box .label { font-size: 0.82rem; color: rgba(255,255,255,0.55); }

  /* FOOTER */
  footer {
    background: var(--dark);
    color: rgba(255,255,255,0.45);
    padding: 3rem 5vw;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
  }
  .footer-logo {
    font-family: 'Playfair Display', serif;
    font-size: 1.3rem;
    font-weight: 900;
  }
  .footer-logo span { color: var(--gold); }
  footer p { font-size: 0.82rem; }
  .footer-links { display: flex; gap: 1.5rem; }
  .footer-links a {
    color: rgba(255,255,255,0.45);
    text-decoration: none;
    font-size: 0.82rem;
    transition: color 0.2s;
  }
  .footer-links a:hover { color: var(--gold); }

  /* ANIMATIONS */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  /* DIVIDER */
  .divider {
    width: 60px; height: 3px;
    background: var(--gold);
    border-radius: 2px;
    margin-bottom: 1.2rem;
  }

  /* RESPONSIVE */
  @media (max-width: 900px) {
    .hero { grid-template-columns: 1fr; padding-top: 100px; min-height: auto; padding-bottom: 4rem; }
    .about-section { grid-template-columns: 1fr; }
    .mosaic { grid-template-columns: 1fr 1fr; }
    .mosaic-item:first-child { grid-column: 1; grid-row: 1; }
    .gallery-grid { grid-template-columns: 1fr 1fr; }
    nav { padding: 0 4vw; }
    .nav-links { gap: 1.2rem; }
  }

  @media (max-width: 600px) {
    .nav-links a:not(.nav-cta) { display: none; }
    .hero h1 { font-size: 2.4rem; }
    .search-grid { grid-template-columns: 1fr; }
    .gallery-grid { grid-template-columns: 1fr; }
    .gallery-item:nth-child(3) { grid-column: span 1; }
    .about-stats { grid-template-columns: 1fr 1fr; }
    footer { flex-direction: column; text-align: center; }
  }
</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- NAV -->
<nav id="navbar">
  <a href="#" class="logo"><span>Route</span><span>Master</span></a>
  <div class="nav-links">
    <a href="#tours">Tours</a>
    <a href="#cabs">Cab Routes</a>
    <a href="#about">About</a>
    <a href="#contact" class="nav-cta">Book Now</a>
  </div>
</nav>

<!-- HERO -->
<div class="hero">
  <div class="hero-bg"></div>
  <div class="hero-content">
    <div class="hero-badge">Maharashtra's Trusted Travel Partner</div>
    <h1>Your Next <em>Adventure</em> Starts Here</h1>
    <p>Explore Maharashtra's hidden gems — from serene Bhigwan birdwatching to Tadoba's wild tigers and Konkan's golden coast. Fast, safe, affordable.</p>
    <div class="hero-actions">
      <a href="tel:9876543210" class="btn-primary">📞 Call to Book</a>
      <a href="#tours" class="btn-outline">Explore Tours →</a>
    </div>
  </div>

  <div class="hero-visual">
    <div class="search-card">
      <h3>Find Your Journey</h3>
      <div class="search-grid">
        <div class="search-field full">
          <label>Destination</label>
          <input type="text" id="place" placeholder="e.g. Tadoba, Bhigwan, Konkan…">
        </div>
        <div class="search-field">
          <label>Departure Date</label>
          <input type="date">
        </div>
        <div class="search-field">
          <label>Return Date</label>
          <input type="date">
        </div>
        <button class="search-btn" onclick="search()">Search Available Tours</button>
      </div>
      <div class="trust-badges">
        <div class="trust-badge"><span>✓</span> Safe Rides</div>
        <div class="trust-badge"><span>✓</span> Best Prices</div>
        <div class="trust-badge"><span>✓</span> Expert Guides</div>
      </div>
    </div>
  </div>
</div>

<!-- TOURS -->
<div class="section-wrap" id="tours">
  <div class="reveal">
    <div class="section-label">Curated Experiences</div>
    <div class="divider"></div>
    <h2 class="section-title">Popular Tours</h2>
    <p class="section-sub">Hand-picked destinations across Maharashtra, crafted for every kind of traveller.</p>
  </div>
  <div class="tours-grid reveal">
    <div class="tour-card">
      <img src="https://images.unsplash.com/photo-1598765271757-f4b2a95f8b2e?w=800&q=90&auto=format&fit=crop" alt="Tadoba Tiger">
      <div class="tour-overlay"></div>
      <div class="tour-info">
        <div class="tour-tag">Wildlife</div>
        <h3>Tadoba</h3>
        <p>Tiger safaris & jungle stays</p>
      </div>
    </div>
    <div class="tour-card">
      <img src="https://images.unsplash.com/photo-1505118380757-91f5f5632de0?w=800&q=90&auto=format&fit=crop" alt="Konkan Beach">
      <div class="tour-overlay"></div>
      <div class="tour-info">
        <div class="tour-tag">Coastal</div>
        <h3>Konkan</h3>
        <p>Pristine beaches & seafood</p>
      </div>
    </div>
    <div class="tour-card">
      <img src="https://images.unsplash.com/photo-1552728089-57bdde30beb3?w=800&q=90&auto=format&fit=crop" alt="Bhigwan Birds">
      <div class="tour-overlay"></div>
      <div class="tour-info">
        <div class="tour-tag">Nature</div>
        <h3>Bhigwan</h3>
        <p>Flamingos & birdwatching</p>
      </div>
    </div>
  </div>
</div>

<!-- CAB ROUTES -->
<div class="section-wrap routes-section" id="cabs">
  <div class="reveal">
    <div class="section-label">Intercity Travel</div>
    <div class="divider"></div>
    <h2 class="section-title">Cab Routes</h2>
    <p class="section-sub" style="color:rgba(255,255,255,0.55)">Comfortable, punctual intercity cab service with more destinations coming soon.</p>
  </div>
  <div class="routes-grid reveal">
    <div class="route-card">
      <img src="https://images.unsplash.com/photo-1566552881560-0be862a7c445?w=800&q=90&auto=format&fit=crop" alt="Pune to Mumbai">
      <div class="route-info">
        <div class="route-from-to">
          <span class="city">Pune</span>
          <span class="route-arrow">→</span>
          <span class="city">Mumbai</span>
        </div>
        <p class="route-detail">Pick-up in Pune · ~3.5 hrs</p>
        <span class="route-badge">Available Now</span>
      </div>
    </div>
    <div class="route-card">
      <img src="https://images.unsplash.com/photo-1570168007204-dfb528c6958f?w=800&q=90&auto=format&fit=crop" alt="Mumbai to Pune">
      <div class="route-info">
        <div class="route-from-to">
          <span class="city">Mumbai</span>
          <span class="route-arrow">→</span>
          <span class="city">Pune</span>
        </div>
        <p class="route-detail">Pick-up in Mumbai · ~3.5 hrs</p>
        <span class="route-badge">Available Now</span>
      </div>
    </div>
    <div class="route-card" style="opacity:0.6;">
      <img src="https://images.unsplash.com/photo-1519955266818-0231f2a7b342?w=800&q=90&auto=format&fit=crop" alt="New Route">
      <div class="route-info">
        <div class="route-from-to">
          <span class="city">New Route</span>
        </div>
        <p class="route-detail">Expanding across Maharashtra</p>
        <span class="route-badge" style="background:rgba(255,255,255,0.1);color:rgba(255,255,255,0.45);border-color:rgba(255,255,255,0.15)">Coming Soon</span>
      </div>
    </div>
  </div>
</div>

<!-- DESTINATIONS -->
<div class="section-wrap" id="destinations">
  <div class="reveal">
    <div class="section-label">Around the World</div>
    <div class="divider"></div>
    <h2 class="section-title">Popular Destinations</h2>
    <p class="section-sub">From Agra to Morocco — dream trips for every wanderer.</p>
  </div>
  <div class="mosaic reveal">
    <div class="mosaic-item">
      <img src="https://images.unsplash.com/photo-1587135941948-670b381f08ce?w=800&q=90&auto=format&fit=crop" alt="Machu Picchu">
    </div>
    <div class="mosaic-item">
      <img src="https://images.unsplash.com/photo-1590073242678-70ee3fc28e8e?w=800&q=90&auto=format&fit=crop" alt="Turkey Coast">
    </div>
    <div class="mosaic-item">
      <img src="https://images.unsplash.com/photo-1539020140153-e479b8f22986?w=800&q=90&auto=format&fit=crop" alt="Morocco">
    </div>
    <div class="mosaic-item">
      <img src="https://images.unsplash.com/photo-1564507592333-c60657eea523?w=800&q=90&auto=format&fit=crop" alt="Agra Taj Mahal">
    </div>
    <div class="mosaic-item">
      <img src="https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?w=800&q=90&auto=format&fit=crop" alt="Japan">
    </div>
  </div>
</div>

<!-- GALLERY -->
<div class="section-wrap" style="background:#F5EEE3;" id="gallery">
  <div class="reveal">
    <div class="section-label">Memories Made</div>
    <div class="divider"></div>
    <h2 class="section-title">Gallery</h2>
    <p class="section-sub">Moments from our travellers across Maharashtra and beyond.</p>
  </div>
  <div class="gallery-grid reveal">
    <div class="gallery-item">
      <img src="https://images.unsplash.com/photo-1469474968028-56623f02e42e?w=800&q=90&auto=format&fit=crop" alt="Mountains">
    </div>
    <div class="gallery-item">
      <img src="https://images.unsplash.com/photo-1500534314209-a25ddb2bd429?w=800&q=90&auto=format&fit=crop" alt="Road Trip">
    </div>
    <div class="gallery-item">
      <img src="https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?w=1200&q=90&auto=format&fit=crop" alt="Landscape">
    </div>
    <div class="gallery-item">
      <img src="https://images.unsplash.com/photo-1488085061387-422e29b40080?w=800&q=90&auto=format&fit=crop" alt="Travel">
    </div>
    <div class="gallery-item">
      <img src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=90&auto=format&fit=crop" alt="Lake">
    </div>
    <div class="gallery-item">
      <img src="https://images.unsplash.com/photo-1501785888041-af3ef285b470?w=800&q=90&auto=format&fit=crop" alt="Sunset">
    </div>
  </div>
</div>

<!-- GUIDES -->
<div class="section-wrap guides-section" id="guides">
  <div class="reveal">
    <div class="section-label">Meet the Team</div>
    <div class="divider"></div>
    <h2 class="section-title">Our Local Guides</h2>
    <p class="section-sub">Experienced, friendly, and passionate about showing you the best of Maharashtra.</p>
  </div>
  <div class="guides-grid reveal">
    <div class="guide-card">
      <img src="https://randomuser.me/api/portraits/men/1.jpg" alt="John">
      <h4>John</h4>
      <p>Wildlife & Safari Expert</p>
      <div class="guide-stars">★★★★★</div>
    </div>
    <div class="guide-card">
      <img src="https://randomuser.me/api/portraits/women/2.jpg" alt="Anna">
      <h4>Anna</h4>
      <p>Coastal Tour Specialist</p>
      <div class="guide-stars">★★★★★</div>
    </div>
    <div class="guide-card">
      <img src="https://randomuser.me/api/portraits/men/3.jpg" alt="David">
      <h4>David</h4>
      <p>Birdwatching & Nature Guide</p>
      <div class="guide-stars">★★★★☆</div>
    </div>
  </div>
</div>

<!-- ABOUT -->
<div class="section-wrap about-section" id="about">
  <div class="about-content reveal">
    <div class="section-label">Who We Are</div>
    <div class="divider" style="background:var(--gold-light)"></div>
    <h2 class="section-title">Explore महाराष्ट्र with Us</h2>
    <p>Discover the beauty of Maharashtra with thoughtfully curated tours that bring you closer to nature, culture, and unforgettable experiences. From the serene birdwatching haven of <strong>Bhigwan</strong> to the thrilling wildlife safaris of <strong>Tadoba</strong>, and the breathtaking coastal charm of the <strong>Konkan</strong> region.</p>
    <p>Whether you're seeking peaceful landscapes, adventure-filled getaways, or hidden gems off the beaten path, we make every journey smooth and memorable.</p>
    <p>Our reliable intercity cab service currently operates between <strong>Pune</strong> and <strong>Mumbai</strong>, ensuring safe, comfortable, and timely journeys — with more destinations coming soon.</p>
    <div style="margin-top:2rem;">
      <a href="tel:9876543210" class="btn-primary" style="background:var(--gold);">📞 Book Your Trip</a>
    </div>
  </div>
  <div class="about-stats reveal" id="about-right">
    <div class="stat-box">
      <div class="number">500+</div>
      <div class="label">Happy Travellers</div>
    </div>
    <div class="stat-box">
      <div class="number">3+</div>
      <div class="label">Popular Tour Packages</div>
    </div>
    <div class="stat-box">
      <div class="number">2</div>
      <div class="label">Intercity Routes</div>
    </div>
    <div class="stat-box">
      <div class="number">24/7</div>
      <div class="label">Customer Support</div>
    </div>
  </div>
</div>

<!-- FOOTER -->
<footer id="contact">
  <div class="footer-logo">Route<span>Master</span></div>
  <p>© 2026 RouteMaster. All rights reserved.</p>
  <div class="footer-links">
    <a href="#tours">Tours</a>
    <a href="#cabs">Cabs</a>
    <a href="#about">About</a>
    <a href="tel:9876543210">Contact</a>
  </div>
</footer>

<script>
  // Smooth cursor
  const cursor = document.getElementById('cursor');
  const cursorRing = document.getElementById('cursorRing');
  let mx = window.innerWidth/2, my = window.innerHeight/2;
  let cx = mx, cy = my, rx = mx, ry = my;

  window.addEventListener('pointermove', e => { mx = e.clientX; my = e.clientY; });

  function animateCursor() {
    cx += (mx - cx) * 0.18;
    cy += (my - cy) * 0.18;
    rx += (mx - rx) * 0.08;
    ry += (my - ry) * 0.08;
    cursor.style.transform = `translate3d(${cx - 7}px, ${cy - 7}px, 0)`;
    cursorRing.style.transform = `translate3d(${rx - 20}px, ${ry - 20}px, 0)`;
    requestAnimationFrame(animateCursor);
  }
  animateCursor();

  // Hover effects
  document.querySelectorAll('a, button, .tour-card, .route-card, .guide-card').forEach(el => {
    el.addEventListener('mouseenter', () => { cursor.style.transform += ' scale(1.8)'; cursor.style.background = '#B84A1E'; });
    el.addEventListener('mouseleave', () => { cursor.style.background = '#C8973A'; });
  });

  // Navbar scroll
  const navbar = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    navbar.classList.toggle('scrolled', window.scrollY > 30);
  });

  // Reveal on scroll
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver(entries => {
    entries.forEach((entry, i) => {
      if (entry.isIntersecting) {
        setTimeout(() => entry.target.classList.add('visible'), i * 80);
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });
  reveals.forEach(el => observer.observe(el));

  // Search
  function search() {
    const place = document.getElementById('place').value.trim();
    if (!place) { alert('Please enter a destination'); return; }
    alert(`Searching tours for: ${place}\n\nCall us at 9876543210 to book!`);
  }
</script>
</body>
</html>
